* {
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', sans-serif; /* Mengganti font agar terlihat lebih modern */
    margin: 0;
    padding: 0;
    color: #333;
    background-color: #f3f4f6; /* Background yang lebih soft */
}

header {
    background: linear-gradient(45deg, #007BFF, #0056b3); /* Gradasi warna biru */
    color: white;
    padding: 20px 0;
    text-align: center;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2); /* Membuat shadow lebih dalam */
    border-bottom: 3px solid #ffcc00; /* Garis bawah yang menarik */
}

nav ul {
    list-style: none;
    padding: 0;
}

nav ul li {
    display: inline-block; /* Mengubah menjadi block untuk kontrol lebih baik */
    margin: 0 20px;
}

nav a {
    color: white;
    text-decoration: none;
    font-weight: 600;
    padding: 8px 15px;
    border-radius: 20px;
    transition: background-color 0.3s ease, color 0.3s ease;
}

nav a:hover {
    background-color: #ffcc00; /* Warna hover dengan background */
    color: #0056b3; /* Warna font hover lebih menarik */
}

section {
    padding: 40px 20px;
    margin: 30px auto;
    max-width: 850px; /* Lebar sedikit lebih besar */
    background-color: #fff;
    border-radius: 12px; /* Membulatkan sudut lebih lagi */
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* Shadow yang lebih lembut */
}

#beranda img {
    border-radius: 50%;
    width: 150px;
    height: 150px;
    transition: transform 0.4s ease, box-shadow 0.4s ease;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Menambahkan shadow pada gambar */
}

#beranda img:hover {
    transform: scale(1.15); /* Sedikit lebih besar pada hover */
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2); /* Membuat shadow lebih dalam pada hover */
}

.proyek {
    background-color: #f7f7f7; /* Background yang lebih lembut */
    border-radius: 10px;
    padding: 20px;
    margin: 20px 0;
    box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.proyek:hover {
    transform: translateY(-8px); /* Lebih mengangkat saat hover */
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15); /* Shadow yang lebih intens saat hover */
}

form {
    display: flex;
    flex-direction: column;
}

form label {
    margin-bottom: 5px;
    font-weight: bold; /* Menambah tebal pada label */
}

form input, form textarea {
    padding: 12px;
    border-radius: 6px;
    border: 1px solid #ccc;
    margin-bottom: 15px;
    font-family: 'Poppins', sans-serif;
    font-size: 14px; /* Menambahkan font yang lebih elegan */
    transition: border-color 0.3s ease;
}

form input:focus, form textarea:focus {
    border-color: #007BFF; /* Border warna biru saat fokus */
    box-shadow: 0 0 5px rgba(0, 123, 255, 0.3); /* Efek shadow saat fokus */
}

form button {
    padding: 12px;
    border: none;
    border-radius: 6px;
    background-color: #007BFF; /* Warna biru cerah */
    color: white;
    font-weight: bold;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.2s ease;
}

form button:hover {
    background-color: #0056b3; /* Warna hover lebih gelap */
    transform: translateY(-3px); /* Efek hover */
}
